První normální forma (1NF) je základní stupeò normalizace databáze, 
kterı se zabıvá odstranìním duplicitních dat. Cílem 1NF je zajistit, 
aby kadı atribut (sloupec) v tabulce mìl jen jednu hodnotu a e se ádná data neopakují. 

Toto zajišuje, e data v databázi jsou jednoznaènì identifikovatelná 
a e je moné vytváøet pøímé vazby mezi tabulkami pomocí cizích klíèù.

První normální forma (1NF) je nejniší z normálních forem a zajišuje, 
e v databázi nejsou ádné redundantní nebo nesprávnì strukturované údaje. 
Je zamìøená na to, aby byla data uloena ve formì tabulky a aby v tabulce byly 
pouze hodnoty jedné entity a aby v tabulce byla pouze jednoduchá data.

1NF se snaí zajistit, aby byly všechny údaje v databázi uloeny v tabulce 
a aby kadı sloupec tabulky mìl pouze jednu hodnotu. To znamená, e pokud 
tabulka obsahuje více sloupcù, kadı sloupec by mìl obsahovat pouze jednu 
hodnotu a nemìl by obsahovat ádné sloité nebo více hodnot.

kritéria:

1.	Kadı atribut (sloupec) v tabulce má jen jednu hodnotu. To znamená, 
e v ádném sloupci nesmí bıt více hodnot nebo opakování stejnıch hodnot.
2.	Kadı záznam (øádek) je unikátní. To znamená, 
e v ádném øádku nesmí bıt opakování stejnıch dat.
3.	Všechny atributy (sloupce) musí bıt atomární. 
To znamená, e nesmí bıt rozdìleny na menší èásti. Napøíklad atribut "jméno" by mìl bıt uloen 
jako jednotlivı sloupec s hodnotou "Jan Novák" namísto toho, aby byl rozdìlen na 
dva sloupce "jméno" a "pøíjmení" s hodnotami "Jan" a "Novák".
